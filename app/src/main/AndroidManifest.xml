<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdejaegere.comimagev1_4">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:noHistory="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.StartActivity"><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".activities.ChoixPictoActivity" />
        <activity android:name=".activities.DeleteSingleQuestionActivity" />
        <activity android:name=".activities.ModifiySingleQuestionActivity" />
        <activity android:name=".activities.ManageSingleQuestionActivity" />
        <activity android:name=".activities.CreditsActivity" />
        <activity android:name=".activities.ManageProfilActivity" />
        <activity android:name=".activities.ManageQuestionsActivity" />
        <activity android:name=".activities.CreateQuestionActivity" />
        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.LoginActivity"/>
    </application>

</manifest>